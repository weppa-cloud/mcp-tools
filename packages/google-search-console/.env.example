# Google Search Console MCP Configuration

# Path to Google credentials JSON file
# Can be either a service account key or OAuth2 credentials
GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# The site URL as registered in Google Search Console
# Must match exactly (including protocol and trailing slash if present)
# Examples:
# - https://example.com/
# - http://example.com/
# - sc-domain:example.com (for domain properties)
GSC_SITE_URL=https://your-website.com/

# Optional: OAuth2 token path (if using OAuth2 instead of service account)
# GOOGLE_OAUTH_TOKEN_PATH=/path/to/token.json

# Optional: Enable debug logging
# DEBUG=true